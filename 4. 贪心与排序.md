# 贪心

### 例1. [luogu-P1181](https://www.luogu.com.cn/problem/P1181)

```c++
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;

    int cnt = 0, sum = 0;
    while (n--)
    {
        int num;
        cin >> num;

        sum += num;
        if (sum > m)
        {
            sum = num;
            cnt++;
        }
    }
    if (sum <= m)
        cnt++;

    cout << cnt;

    return 0;
}
```

## 贪心的注意事项

贪心的结果不一定正确, 要得到正确的结果, 需要保证贪心结果无后效性.

## 贪心 + 堆

### 例2. [luogu-P1090](https://www.luogu.com.cn/problem/P1090)

```c++
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;

    if (n == 1)
    {
        int weight;
        cin >> weight;
        cout << weight;
        return 0;
    }

    priority_queue<int, vector<int>, greater<int> > weights;
    while (n--)
    {
        int weight;
        cin >> weight;
        weights.push(weight);
    }

    int sum = 0;
    while (weights.size() > 1)
    {
        int w1 = weights.top();
        weights.pop();
        int w2 = weights.top();
        weights.pop();

        sum += w1 + w2;
        weights.push(w1 + w2);
    }

    cout << sum;

    return 0;
}
```

## 贪心 + 二分

### **例3.** [**luogu-P1182**](https://www.luogu.com.cn/problem/P1182)

```c++
#include <bits/stdc++.h>
using namespace std;

int n, m;
const int MAXSIZE = 100005;
int s[MAXSIZE];

bool judge(int sum)
{
    int anchor = 0, cur, cnt = 0;
    for (cur = 1; cur <= n; cur++)
    {
        if (s[cur] - s[anchor] <= sum)
            continue;
        anchor = --cur;
        cnt++;
    }
    return cnt >= m;
}

int main()
{
    cin >> n >> m;

    int left = 0, right;
    for (int i = 1; i <= n; i++)
    {
        int num;
        cin >> num;
        s[i] = s[i - 1] + num;
        left = max(left, num);
    }
    right = s[n];

    while (left <= right)
    {
        int mid = left + (right - left) / 2;
        if (judge(mid))
            left = mid + 1;
        else
            right = mid - 1;
    }

    cout << left;

    return 0;
}
```


# 排序

[`sort()`](http://www.cplusplus.com/reference/algorithm/sort/) 和 [`stable_sort()`](http://www.cplusplus.com/reference/algorithm/stable_sort/) 分别进行非稳定排序和稳定排序.

[`nth_element()`](http://www.cplusplus.com/reference/algorithm/nth_element/) 可以将区间内的第 $k$ 小的元素放在最终位置, 其中, 小包含了自定义的 `cmp()` 函数所定义的"小".


# 习题链接
🔗 https://vjudge.net/contest/356986

### 1. [HDUOJ-1009](http://acm.hdu.edu.cn/showproblem.php?pid=1009)

```c++
#include <bits/stdc++.h>
using namespace std;

struct room {
    int j, f;
    double price;
};

bool cmp(const room& r1, const room& r2)
{
    return r1.price < r2.price;
}

int main()
{
    int m, n;
    while (cin >> m >> n)
    {
        if (m == -1 && n == -1)
            break;

        vector<room> rooms(n);
        for (int i = 0; i < n; i++)
        {
            cin >> rooms[i].j >> rooms[i].f;
            rooms[i].price = (double)rooms[i].f / rooms[i].j;
        }
        sort(rooms.begin(), rooms.end(), cmp);

        double gain = 0.0;
        for (int i = 0; i < n; i++)
        {
            if (m >= rooms[i].f)
            {
                gain += rooms[i].j;
                m -= rooms[i].f;
            }
            else
            {
                gain += m / rooms[i].price;
                break;
            }
        }
        printf("%.3f\n", gain);
    }

    return 0;
}
```

### 2. [HDUOJ-1050](http://acm.hdu.edu.cn/showproblem.php?pid=1050)

```c++
#include <bits/stdc++.h>
using namespace std;

const int MAXSIZE = 200;
int cnt[MAXSIZE];

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int n;
        cin >> n;

        memset(cnt, 0, sizeof(cnt));
        int mostUsed = 0;
        for (int i = 0; i < n; i++)
        {
            int start, dest, left, right;
            cin >> start >> dest;
            left = (min(start, dest) - 1) / 2;
            right = (max(start, dest) - 1) / 2;
            for (int k = left; k <= right; k++)
                mostUsed = max(mostUsed, ++cnt[k]);
        }
        cout << 10 * mostUsed << endl;
    }

    return 0;
}
```

### 3. [HDUOJ-2037](http://acm.hdu.edu.cn/showproblem.php?pid=2037)

```c++
#include <bits/stdc++.h>
using namespace std;

struct tvshow {
    int s, e, t;
};

bool cmp(const tvshow& a, const tvshow& b)
{
    return a.t != b.t ? a.t < b.t : a.s < b.s;
}

bool overlap(const vector<pair<int, int> >& intervals, const int& ti_s, const int& ti_e)
{
    for (const auto& cur : intervals)
        if (!(cur.first >= ti_e || cur.second <= ti_s))
            return true;
    return false;
}

int main()
{
    int n;
    while (cin >> n)
    {
        if (n == 0)
            break;

        vector<tvshow> shows(n);
        for (int i = 0; i < n; i++)
        {
            cin >> shows[i].s >> shows[i].e;
            shows[i].t = shows[i].e - shows[i].s;
        }
        sort(shows.begin(), shows.end(), cmp);

        int cnt = 0;
        vector<pair<int, int> > occupied;
        for (int i = 0; i < n; i++)
        {
            if (!overlap(occupied, shows[i].s, shows[i].e))
            {
                cnt++;
                occupied.emplace_back(shows[i].s, shows[i].e);
            }
        }
        cout << cnt << endl;
    }

    return 0;
}
```

讨论区题解之一:

```c++
#include <bits/stdc++.h>
using namespace std;

struct tvshow {
    int s, e;
};

bool cmp(const tvshow& a, const tvshow& b)
{
    return a.e < b.e;
}

int main()
{
    int n;
    while (cin >> n && n)
    {
        vector<tvshow> shows(n);
        for (int i = 0; i < n; i++)
            cin >> shows[i].s >> shows[i].e;
        sort(shows.begin(), shows.end(), cmp);

        int cnt = 0, preTi_e = 0;
        for (const auto& show : shows)
        {
            if (show.s >= preTi_e)
            {
                cnt++;
                preTi_e = show.e;
            }
        }
        cout << cnt << endl;
    }

    return 0;
}
```

### 4. [HDUOJ-4550](http://acm.hdu.edu.cn/showproblem.php?pid=4550)

```c++
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        string cards, res;
        cin >> cards;

        int minPos;
        char minChar = '9' + 1;

        for (int i = cards.length() - 1; i >= 0; i--)
        {
            if (cards[i] != '0' && cards[i] < minChar)
            {
                minChar = cards[i];
                minPos = i;
            }
        }

        res.push_back(*cards.begin());
        for (int i = 1; i <= minPos; i++)
        {
            if (i < minPos)
            {
                if (cards[i] > res[0])
                    res.push_back(cards[i]);
                else
                    res.insert(res.begin(), cards[i]);
            }
            else
                res.insert(res.begin(), cards[i]);
        }
        res += cards.substr(minPos + 1);

        cout << res << endl;
    }

    return 0;
}
```

### 5. [CodeForces 749A](http://codeforces.com/problemset/problem/749/A)

```c++
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;

    cout << n / 2 << endl;
    if (n & 1)
    {
        for (int i = 0; i < n / 2 - 1; i++)
            cout << 2 << " ";
        cout << 3;
    }
    else
    {
        for (int i = 0; i < n / 2; i++)
            cout << 2 << " ";
    }

    return 0;
}
```

### 6. [CodeForces 749B](http://codeforces.com/problemset/problem/749/B)

```c++
#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<pair<int, int> > vertices(3);
    for (int i = 0; i < 3; i++)
        cin >> vertices[i].first >> vertices[i].second;

    cout << 3 << endl;
    cout << vertices[2].first + vertices[1].first - vertices[0].first << " "
         << vertices[2].second + vertices[1].second - vertices[0].second << endl;
    cout << vertices[2].first - vertices[1].first + vertices[0].first << " "
         << vertices[2].second - vertices[1].second + vertices[0].second << endl;
    cout << vertices[1].first + vertices[0].first - vertices[2].first << " "
         << vertices[1].second + vertices[0].second - vertices[2].second << endl;

    return 0;
}
```

### 7. [CodeForces 749C](http://codeforces.com/problemset/problem/749/C)

```c++
#include <bits/stdc++.h>
using namespace std;

string employees;
queue<int> d, r;

int main()
{
    int n;
    cin >> n >> employees;
    for (int i = 0; i < n; i++)
    {
        if (employees[i] == 'D')
            d.push(i);
        else
            r.push(i);
    }

    while (!d.empty() && !r.empty())
    {
        int idx_d = d.front(), idx_r = r.front();
        d.pop(), r.pop();
        if (idx_d < idx_r)
            d.push(idx_d + n);
        else
            r.push(idx_r + n);
    }
    if (d.empty())
        cout << "R";
    else
        cout << "D";

    return 0;
}
```

### 8. [CodeForces 491B](http://codeforces.com/problemset/problem/491/B)

```c++
#include <bits/stdc++.h>
using namespace std;

typedef long long LL;
int n, m, c, h;
LL nw = LLONG_MIN, ne = LLONG_MIN, sw = LLONG_MIN, se = LLONG_MIN;

int main()
{
    cin >> n >> m >> c;
    LL x, y;
    /*
     * 对任意的餐厅而言, 只需计算其与最偏远四个角的酒店的曼哈
     * 顿距离, 这四个距离的最大者为该餐厅到最远的酒店的距离.
     * 因此设置四个变量, 表示曼哈顿距离计算式与酒店坐标有关的
     * 部分.
    **/
    for (int i = 0; i < c; i++)
    {
        cin >> x >> y;
        nw = max(nw, -x + y);
        ne = max(ne, x + y);
        sw = max(sw, -x - y);
        se = max(se, x - y);
    }
    cin >> h;
    LL dis = LLONG_MAX, pos;
    for (int i = 1; i <= h; i++)
    {
        cin >> x >> y;
        /*
         * 对于距当前餐厅最西北的酒店, 设其坐标为(x0, y0),
         * 则二者之间的曼哈顿距离为
         * |x - x0| + |y - y0| == x - y - x0 + y0,
         * 即 x - y + nw, 其余三个方向同理.
        **/
        LL maxDis = max(max(x - y + nw, -x - y + ne), max(x + y + sw, -x + y + se));
        // 若最远距离更小, 则更新最远距离, 并记录该餐厅编号.
        if (maxDis < dis)
        {
            dis = maxDis;
            pos = i;
        }
    }
    cout << dis << endl << pos;

    return 0;
}
```

### 9. [CodeForces 754A](http://codeforces.com/problemset/problem/754/A)

```c++
#include <bits/stdc++.h>
using namespace std;

int n, sum = 0;

int main()
{
    cin >> n;
    int a, pos = 0;
    for (int i = 1; i <= n; i++)
    {
        cin >> a;
        sum += a;
        if (a != 0)
            pos = i;
    }

    if (pos == 0)
        cout << "NO\n";
    else if (sum != 0)
        cout << "YES\n" << 1 << endl
             << 1 << " " << n << endl;
    else
        cout << "YES\n" << 2 << endl
             << 1 << " " << pos - 1 << endl
             << pos << " " << n << endl;

    return 0;
}
```

### 10. [CodeForces 757B](http://codeforces.com/problemset/problem/757/B)

### 11. [CodeForces 762B](http://codeforces.com/problemset/problem/762/B)

### 12. [CodeForces 766B](http://codeforces.com/problemset/problem/766/B)